import { Injectable } from '@angular/core';
 

@Injectable()
export class ReportService {
    //dates are in yyyy-MM-dd format
    buildPercentageCompleted(startDate : string, endDate : string) : number{
        return 0;
    }
    
    buildIncidentsByOwnerArea(){
        return;
    }
}